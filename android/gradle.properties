# gradle.properties — PALM RAG Gradle Project Properties
# Configures AndroidX migration flag and JVM memory allocation for the Gradle daemon.

android.useAndroidX=true        # Enable AndroidX libraries instead of legacy Android Support libraries
org.gradle.jvmargs=-Xmx2048m   # Allocate 2GB max heap memory to the Gradle daemon JVM process
