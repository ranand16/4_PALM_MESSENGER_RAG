// settings.gradle — PALM RAG Project Settings
// Defines which Gradle modules are included in the build and the root project name.

include ':app'                    // Include the :app module (android/app/) in the build
rootProject.name = "PalmRAG"      // Set the root project name (used in build output paths)
